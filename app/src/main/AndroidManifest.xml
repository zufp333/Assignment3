<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zufpilosof.assignment3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/play_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight">
        <activity android:name=".SignInActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ServiceProvidersMainActivity"></activity>
        <activity android:name=".ServiceProviderDetailsActivity"></activity>
        <activity android:name=".ReviewActivity"></activity>

    </application>

</manifest>